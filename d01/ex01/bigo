>> O(n) time complexity - this function recursively traverses through the list
	once.
>> O(n) space complexity - for each recursive call, another instance of the
	function is initialized on the stack
